# get hashtags off a Tweet object
# returning tweets using #hashtags method on a tweet obj will return a
#    Hashtag(s) object hash with tags inside of it.

hashtags_obj.each do |h|
	puts h.text # prints
end

